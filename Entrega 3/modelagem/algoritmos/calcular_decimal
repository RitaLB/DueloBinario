Calcular_decimal( linha: int, coluna: int)
 
// Posição no tabuleiro (coordenadas) dos dígitos binários
pos_digito_0 = (linha, coluna + 1)
pos_digito_1 = (linha +1 , coluna)
pos_digito_2 = (linha, coluna -1)
pos_digito_3 = (linha -1, coluna)

// consultar no tabuleiro valor dos digitos na posição calculada
digito_0 = tabuleiro[pos_digito_0[1]][pos_digito_0[0]]
digito_1 = tabuleiro[pos_digito_1[1]][pos_digito_1[0]]
digito_2 = tabuleiro[pos_digito_2[1]][pos_digito_2[0]]
digito_3 = tabuleiro[pos_digito_3[1]][pos_digito_3[0]]

// binario formado pela concatenação dos dígidos do bit  + significativo (digito 3) ao menos significativo (digito 0)
binario: string = digito_3 + digito_2  + digito_1 + digito_0 

// Transforma no correspondente decial à string binaria
decimal_inicial = int(binario)

// decimal final corresponde à soma do valor das dezenas e das unidades
unidades = decimal_inicial - 10

if unidades >= 0
	decimal = unidades + 1
else
	decimal = unidades
	
return decimal
